{
  "name": "next-img-docs",
  "scripts": {
    "dev": "next",
    "build": "next build && next export && touch out/.nojekyll",
    "clean": "rm -rf .next && rm -rf out",
    "start": "next start",
    "release:docs": "npm run clean && npm run build && gh-pages -d out -t"
  },
  "dependencies": {
    "js-beautify": "^1.14.9",
    "next": "^13.4.12",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-syntax-highlighter": "^15.5.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
