{
  "name": "next-img-example",
  "scripts": {
    "dev": "next",
    "build": "next build && next export && touch out/.nojekyll",
    "clean": "rm -rf .next && rm -rf out",
    "start": "next start",
    "release:docs": "npm run clean && npm run build && gh-pages -d out -t"
  },
  "dependencies": {
    "@mdx-js/loader": "^1.6.5",
    "@next/mdx": "^9.4.4",
    "js-beautify": "^1.11.0",
    "next": "^9.3.5",
    "next-compose-plugins": "^2.2.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-syntax-highlighter": "^12.2.1"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0"
  }
}
